/**
 * API base URL. Must end with a trailing slash; the backend serves under /api.
 * For a production deployment, change this to your deployed API origin
 * (e.g. 'https://your-api.example.com/api/').
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api/',
};
