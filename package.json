{
  "name": "cocktailapp",
  "version": "1.0.0",
  "description": "CocktailApp — reconstructed & modernized full-stack app (Angular + NestJS + MongoDB Atlas).",
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "shared",
    "backend",
    "frontend",
    "app"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace @cocktailapp/shared",
    "validate:seed": "node scripts/validate-seed.mjs",
    "build:translations": "node scripts/build-translations-nl.mjs",
    "build:catalog": "npm run validate:seed && node scripts/build-catalog.mjs",
    "build": "npm run build:shared && npm run build --workspace backend && npm run build --workspace frontend",
    "build:app-web": "npm run build:shared && npm run build:catalog && npm run export:web --workspace @cocktailapp/app",
    "start:backend": "npm run start:dev --workspace backend",
    "start:frontend": "npm run start --workspace frontend",
    "start:app": "npm run start --workspace @cocktailapp/app",
    "web:app": "npm run web --workspace @cocktailapp/app",
    "dev": "npm run build:shared && concurrently -n backend,frontend -c blue,green \"npm run start:dev --workspace backend\" \"npm run start --workspace frontend\"",
    "db:ping": "node scripts/db-ping.mjs",
    "db:count": "node scripts/db-count.mjs",
    "db:seed": "node scripts/db-seed.mjs",
    "db:shell": "node scripts/db-shell.mjs"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "mongodb-mcp-server": "1.13.0"
  }
}
